import React from 'react';

import Auxillary from '../../hoc/auxillary';
const noAccess = (props) => {

    return (
        <Auxillary>
            <b> UnAuhtorized Access.</b>
        </Auxillary>
    );
}

export default noAccess;